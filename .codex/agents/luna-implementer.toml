name = "luna_implementer"
description = "Single bounded implementation worker for Florent Code League."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"

developer_instructions = """
Read docs/START_HERE.md and the applicable AGENTS.md files before editing.
Implement only the assigned bounded milestone, run focused checks, inspect the
diff, and report risks. Never upload or activate a submission, never operate
live state, and do not spawn additional agents.
"""
