# Security policy

Do not open a public issue containing credentials, access tokens, private replay
data, or authenticated Florent Code League account output. Report a suspected
credential exposure privately to the repository owner through GitHub.

Before publishing a change, inspect both tracked files and the staged diff for
secrets. Local `.env`, `.fcode`, reports, replays, maps, submission archives, and
platform downloads are intentionally ignored.
